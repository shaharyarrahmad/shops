{
  "name": "my-gridsome-site",
  "private": true,
  "scripts": {
    "serve:static": "yarn concurrently \"nodemon --ext js,vue --watch '../../_shared-components/' --exec 'yarn upgrade shared-components && yarn gridsome build && yarn http-server ./dist/'\"",
    "serve:local": "yarn concurrently \"yarn gridsome develop\" \"nodemon --ext js,vue --watch '../../_shared-components/' --exec 'yarn upgrade shared-components'\""
  },
  "resolutions": {
    "graphql": "^14.7.0"
  },
  "dependencies": {
    "@fontsource/roboto-mono": "^4.0.1",
    "pinelab-storefront-client": "0.19.0"
  },
  "devDependencies": {
    "@gridsome/source-graphql": "^0.1.0",
    "concurrently": "^5.3.0",
    "gridsome": "^0.7.23",
    "http-server": "^0.12.3",
    "nodemon": "^2.0.7",
    "webpack-bundle-analyzer": "^4.3.0"
  }
}
