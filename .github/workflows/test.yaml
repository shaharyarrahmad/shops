name: Test live checkout for clients
on:
  schedule:
    - cron:  '0 1 * * *'
  push:
    branches:
      - feat/test-linkinator

jobs:
  test:
    name: ${{ matrix.client[0] }}
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: packages/e2e
    strategy:
      matrix:
        client:
          [
              ['cantastic', 'https://cantastic.netlify.app/' ]
          ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '16'
      - name: Build
        run: yarn
      - name: Test checkout
        run: TESTCASE=${{ matrix.client[0] }} yarn test:checkout
      - name: Check for 404's
        run: yarn linkinator ${{ matrix.client[1] }} --skip '^(?!${{ matrix.client[1] }})' --recurse --verbosity 'error'
