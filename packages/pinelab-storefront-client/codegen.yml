overwrite: true
schema: "https://api.pinelab.studio/shop-api"
config:
  resolverValidationOptions:
    requireResolversForResolveType: false

generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"