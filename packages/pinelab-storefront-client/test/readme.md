# Pinelab Storefront client - Test
This folder is jus for running gridsome with the compiled client, without the need to publish it.

Gridsome needs a package.json, so we have one in test/package.json.