{
  "name": "pinelab-storefront-client-test",
  "private": true,
  "scripts": {
    "serve": "rimraf node_modules && yarn && yarn gridsome develop"
  },
  "dependencies": {
    "@fontsource/roboto-mono": "^4.0.1",
    "pinelab-storefront-client": "file:.."
  },
  "devDependencies": {
    "@gridsome/source-graphql": "^0.1.0",
    "gridsome": "^0.7.23",
    "webpack-bundle-analyzer": "^4.4.1",
    "rimraf": "^3.0.2"
  }
}
