{
  "name": "pinelab-storefront-client",
  "version": "0.19.0",
  "description": "Vendure client for Vue + Gridsome server-side generation",
  "author": "Martijn van de Brug <martijn@pinelab.studio>",
  "homepage": "https://pinelab.studio",
  "repository": "https://github.com/martijnvdbrug/shops/tree/master/packages/vendure-plugin-webhook",
  "license": "MIT",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist && yarn tsc --build && yarn copyfiles -u 1 'src/components/**/*' dist",
    "generate": "graphql-codegen --config codegen.yml",
    "serve": "nodemon --verbose --ext js,ts --watch src --watch test --ignore test/src/.temp --ignore test/.cache --exec 'yarn build && yarn serve:gridsome'",
    "serve:gridsome": "cd test && yarn upgrade pinelab-storefront-client && yarn gridsome develop"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "gridsome-plugin-typescript": "^0.4.0",
    "nodemon": "^2.0.7",
    "typescript": "^4.2.3",
    "vue": "^2.6.12"
  },
  "dependencies": {
    "graphql": "15.3.0",
    "graphql-request": "^3.4.0",
    "vue-class-component": "^7.2.6"
  },
  "resolutions": {
    "graphql": "15.3.0"
  }
}
