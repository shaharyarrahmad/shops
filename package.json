{
  "name": "shops",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "bootstrap": "yarn && yarn lerna exec yarn && yarn lerna run build",
    "prettier:check": "prettier --check \"./**/*.{ts,tsx,js,vue,scss,sass}\"",
    "prettier:write": "prettier --write \"./**/*.{ts,tsx,js,vue}\"",
    "generate-types": "cd packages/common && yarn generate"
  },
  "license": "MIT",
  "devDependencies": {
    "husky": "^6.0.0",
    "lerna": "^4.0.0",
    "prettier": "2.2.1",
    "pretty-quick": "^3.1.0"
  }
}
