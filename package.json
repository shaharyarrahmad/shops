{
  "name": "shops",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "yarn && yarn lerna clean -y && yarn lerna bootstrap && yarn lerna build"
  },
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
