{
  "name": "shops",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "yarn concurrently \"cd vendure && yarn serve:test\" \"cd _gridsome_template && && sleep 15s && yarn serve:local\"",
    "serve:local": "yarn concurrently \"export NODE_ENV=local yarn gridsome develop\" \"nodemon --ext js,vue --watch '../_shared-components/' --exec 'yarn upgrade shared-components'\"",
    "bootstrap": "yarn && yarn lerna clean -y && yarn lerna bootstrap && yarn lerna run build",
    "update-vendure": "lerna exec \"yarn upgrade '@vendure/*'\" --latest"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@vendure/admin-ui": "0.18.3",
    "@vendure/admin-ui-plugin": "0.18.3",
    "@vendure/asset-server-plugin": "0.18.3",
    "@vendure/core": "0.18.3",
    "@vendure/email-plugin": "0.18.3",
    "@vendure/testing": "0.18.3",
    "@vendure/ui-devkit": "0.18.3",
    "concurrently": "^5.3.0",
    "copyfiles": "^2.4.1",
    "jest": "^26.6.3",
    "lerna": "^3.22.1",
    "rimraf": "^3.0.2",
    "typescript": "4.0.5",
    "ts-jest": "^26.5.0"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "vendure/"
    ],
    "nohoist": [
      "**/@vendure/ui-devkit"
    ]
  }
}
