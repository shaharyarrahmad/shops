{
  "name": "shops",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "yarn && yarn lerna clean -y && yarn lerna bootstrap && yarn lerna run build"
  },
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
